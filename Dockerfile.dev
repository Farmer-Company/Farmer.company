FROM node:18-alpine
WORKDIR /app

# Install deps
COPY package.json package-lock.json* ./
RUN npm ci --silent

# Copy source
COPY . .

# Expose Vite dev server
ENV HOST=0.0.0.0 PORT=5173
EXPOSE 5173

CMD ["npm", "run", "dev"]
